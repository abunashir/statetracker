<h3>Object State at <%= @state_tracker.timestamp  %></h3>

<div class="col-md-12 top10">
  <% unless @state_tracker.status.empty? %>
    <pre><%= @state_tracker.status %></pre>
  <% else %>
    <pre><%= @state_tracker.tg_object_type %> <%= @state_tracker.tg_object_id %> Didn't Exist at that time.</pre>
  <% end %>

  <br />
  <%= link_to(
    "Track another object", new_state_tracker_path, class: "btn btn-info"
  ) %>
</div>

